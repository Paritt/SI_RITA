# Pinned to the versions verified on Python 3.11 (macOS dev + Windows/RayStation).
# Install with Python 3.11 on each machine — never copy an installed venv across OS.
langgraph==1.2.9
langchain-core==1.4.9
langchain-openai==1.3.5
langchain-anthropic==1.4.8
langsmith==0.10.2
ttkbootstrap==1.20.4
python-dotenv==1.2.2
